"""Supabase data layer — client, Pydantic schemas, and typed repositories.

Replaces the old app/firebase package. The database schema these repositories
target lives in supabase/migrations/0001_init.sql.
"""
